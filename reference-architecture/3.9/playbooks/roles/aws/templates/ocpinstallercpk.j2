openshift_cloudprovider_kind=aws
openshift_clusterid={{ clusterid }}
{{ lookup('file', ( '~/.ssh/config-' + clusterid + '-cpkuser_access_key') ) | regex_replace(".* ANSIBLE MANAGED BLOCK .*", '') | trim }}
